def switch_to_table_window(main_window,object_window):
    time.sleep(10)
    for handle in driver.window_handles:
        if handle not in [main_window, object_window]:
            driver.switch_to.window(handle)
            print("Switched to table window")
            break
    time.sleep(5)
