Here is the complete list with your new points added, rewritten cleanly and professionally:


---

IQ Execution Steps

1. Verify Input File
Navigate to the Input folder and ensure that the Report file is present.


2. Configure Input File Path
Open the Config file and set the INPUT.FILE entry to point to the correct report file.


3. Validate Parent Types
Check the TYPES entry in the Config:

Ensure all required parent types are listed.

Confirm they are comma-separated.



4. Validate Excluded Child Types
Review the EXCLUDE.TYPE entry:

Make sure all child types to be filtered out are listed.

Confirm they are comma-separated.



5. Run the Script
Navigate to the Script folder and execute the shell script.

TCL 1: Generates type-wise output files in the Output folder using the report and configuration.

TCL 2: Reads each output file and connects the corresponding Legacy Reference Type object with required attributes to the parent specification.



6. Verify Generated Output Files
Go to the Output folder and ensure the output files for the required types have been created.

Open each file and verify the count of objects inside.



7. Check Output Folder Logs
Review the success and error logs in the Output folder.

Confirm the success count matches the expected number.

Note any errors if present.



8. Verify Legacy Document Connection Files
Navigate to the Log folder and confirm that all Legacy Document Connection files have been generated by type.

Open each file and count the objects.

The count should match the corresponding output file count.



9. Check Script Execution Logs
Review the success and error logs in the Log folder:

Verify the success count.

Check for any errors generated during the connection process.





---

If you want, I can convert this into a formal SOP document, a PDF, or a checklist format.
