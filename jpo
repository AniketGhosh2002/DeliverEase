def click_when_ready(by, value, description="", offset=None, wait=30):
    elem = WebDriverWait(driver, wait).until(EC.element_to_be_clickable((by, value)))
    if offset:
        actions.move_to_element_with_offset(elem, *offset).click().perform()
    else:
        actions.move_to_element(elem).click().perform()
    print(f"✅ Clicked {description}")
    time.sleep(5)
    return elem




how can i call for this 
product_type = WebDriverWait(driver, 10).until(
        EC.presence_of_element_located((By.NAME, "TypeActualDisplay"))
    )
actions.move_to_element_with_offset(product_type, 5, 30).click().perform()
    print(f"Selected '{TYPE}' as Type")
