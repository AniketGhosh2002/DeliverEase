def switch_to_recent_window(driver):
    """
    Switch Selenium driver to the most recently opened window
    """
    time.sleep(3)  # small wait to ensure window loads
    driver.switch_to.window(driver.window_handles[-1])
    print("✅ Switched to recent window:", driver.current_window_handle)
    time.sleep(2)
